# ============================================================
# GenAI Research Platform - Requirements
# ChemExtract AI + ReactionLens Chemical Data Extraction
# ============================================================

# ── Core Web Framework ──────────────────────────────────────
Flask>=2.0.0
flask-cors>=3.0.10
flask-limiter>=3.0.0
python-dotenv>=1.0.0
Werkzeug>=2.0.0

# ── PDF Processing ─────────────────────────────────────────
pypdf>=3.0.0
pdfplumber>=0.7.0
PyMuPDF>=1.23.0          # fitz — PDF-to-image & text extraction

# ── Chemistry / Molecular Visualization ───────────────────
rdkit>=2022.3.1           # SMILES validation, molecular rendering

# ── Image Processing (optional, for ReactionLens) ──────────
opencv-python>=4.5.0      # cv2 — enhanced page segmentation
Pillow>=9.0.0             # image handling helpers

# ── Network Analysis ──────────────────────────────────────
networkx>=2.6.0

# ── Data Processing ───────────────────────────────────────
pandas>=1.3.0
numpy>=1.21.0

# ── HTTP Requests ─────────────────────────────────────────
requests>=2.26.0          # synchronous
aiohttp>=3.8.0             # asynchronous
aiohttp[speedups]>=3.8.0   # optional C speed-ups

# ── Chemical Lookup (optional, for ReactionLens resolver) ──
pubchempy>=1.0.2           # PubChem SMILES resolution

# ── Caching (optional, uncomment if using Redis) ──────────
# redis>=4.0.0

# ── Testing ───────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# ── Type Hints & Validation ────────────────────────────────
typing-extensions>=4.0.0

# ── WSGI Server (production, pick one) ────────────────────
gunicorn>=21.0.0
# waitress>=2.1.0
